import myFuncLib as mfl

vector = [1,2,3]
print(mfl.myFunc(vector))

# Numpy needs to be imported into myFuncLib because it needs numpy to perform functions from numpy
# test.py can access nested libraries/module within imported modules using the . operator